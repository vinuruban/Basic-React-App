import React, { Component } from 'react';

class NiceMain extends Component {
  render() {
    return (
      <div>
        <main>This is the main section</main>
      </div>
    );
  }
}

export default NiceMain; //For this component to be available in other places in the application